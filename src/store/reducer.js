import * as actionTypes from './action'

const initialState = {
    ingredients: null,
    totalprice: 4
}

export default (state = initialState, { type, payload }) => {
    switch (type) {

        default:
            return state
    }
}
